# Explore With Me Plus

Групповой проект созданный в рамках учебного курса Java-разработчик расширенный (Яндекс Практикум)

## 📝 Описание проекта

**Explore With Me Plus** — это многомодульное приложение для публикации и поиска событий (аналог афиши).
Пользователи могут создавать события, искать их по различным критериям, оставлять заявки на участие, а также
комментировать события.

Проект состоит из двух основных сервисов:

1. **Stats Service (Сервис статистики):** Собирает и предоставляет статистику по просмотрам событий.
2. **EWM Service (Основной сервис):** Реализует всю бизнес-логику, связанную с пользователями, категориями, событиями,
   подборками, запросами на участие и комментариями.

## 📚 Полная спецификация API

### Основной сервис (EWM)

#### Публичный API (доступ без регистрации)

##### Категории

* `GET /categories` - Получение всех категорий (пагинация)
* `GET /categories/{catId}` - Получение информации о категории по её идентификатору

##### События

* `GET /events` - Получение событий с фильтрацией
    * **Параметры:** `text` (поиск в аннотации и описании), `categories`, `paid`, `rangeStart`, `rangeEnd`,
      `onlyAvailable`, `sort` (EVENT_DATE/VIEWS), `from`, `size`
* `GET /events/{id}` - Получение подробной информации о событии по его идентификатору

##### Подборки событий

* `GET /compilations` - Получение всех подборок (пагинация)
* `GET /compilations/{compId}` - Получение подборки по её идентификатору

#### Закрытый API (доступ авторизованных пользователей)

##### Пользователи

* `GET /users/{userId}/events` - Получение событий, добавленных текущим пользователем
* `POST /users/{userId}/events` - Добавление нового события
* `GET /users/{userId}/events/{eventId}` - Получение полной информации о событии, добавленном текущим пользователем
* `PATCH /users/{userId}/events/{eventId}` - Изменение события, добавленного текущим пользователем
* `GET /users/{userId}/events/{eventId}/requests` - Получение информации о запросах на участие в событии текущего
  пользователя
* `PATCH /users/{userId}/events/{eventId}/requests` - Изменение статуса заявок на участие в событии

##### Запросы на участие

* `GET /users/{userId}/requests` - Получение информации о заявках текущего пользователя на участие в чужих событиях
* `POST /users/{userId}/requests` - Добавление запроса от текущего пользователя на участие в событии
* `PATCH /users/{userId}/requests/{requestId}/cancel` - Отмена своего запроса на участие в событии

#### Административный API (для администраторов)

##### Пользователи

* `GET /admin/users` - Получение информации о пользователях (пагинация, фильтрация по ids)
* `POST /admin/users` - Добавление нового пользователя
* `DELETE /admin/users/{userId}` - Удаление пользователя

##### Категории

* `POST /admin/categories` - Добавление новой категории
* `DELETE /admin/categories/{catId}` - Удаление категории
* `PATCH /admin/categories/{catId}` - Изменение категории

##### События

* `GET /admin/events` - Поиск событий (фильтрация по пользователям, состояниям, датам)
* `PUT /admin/events/{eventId}/publish` - Публикация события
* `PUT /admin/events/{eventId}/reject` - Отклонение события

##### Подборки

* `POST /admin/compilations` - Добавление новой подборки
* `DELETE /admin/compilations/{compId}` - Удаление подборки
* `PATCH /admin/compilations/{compId}` - Обновление подборки

### Сервис статистики (Stats)

#### API для сбора статистики

* `POST /hit` - Сохранение информации о том, что к эндпоинту был запрос

#### API для получения статистики

* `GET /stats` - Получение статистики по посещениям
    * **Параметры:** `start`, `end`, `uris`, `unique` (только уникальные ip)

## 💬 Дополнительная функциональность: Комментарии

### Публичный API

* `GET /events/comments` - Получение всех комментариев с возможностью сортировки по дате.

**Параметры:**

* `sort` - сортировка по дате. Значения: `ASC`, `DESC`. По умолчанию: `ASC`

    **Ответ:** Если комментарии не найдены, возвращает пустой список

* `GET /events/{eventId}/comments` - Получение комментариев к определённому событию.

**Параметры:**

* `sort` - сортировка по дате. Значения: `ASC`, `DESC`. По умолчанию: `ASC`

    **Ответ:** Если комментарии не найдены, возвращает пустой список

### Приватный API (для авторизованных пользователей)

* `GET /users/{userId}/comments` - Получение комментариев, добавленных текущим пользователем.

    **Ответ:** Если комментарии не найдены, возвращает пустой список

* `POST /users/{userId}/comments` - Добавление нового комментария к событию.

**Особенности:** Статус `WAITING` по умолчанию

* `PATCH /users/{userId}/comments` - Изменение комментария пользователем, который его оставил.

**Ошибки:**

* `400` - Если поле `text` не указано, пустое или состоит только из пробелов
* `403` - Пользователь не является автором комментария
* `404` - Комментарий не найден или недоступен

**Особенности:** Статус сбрасывается на `WAITING`

* `DELETE /users/{userId}/comments/{comId}` - Удаление комментария пользователем, который его оставил.

**Ошибки:**

* `404` - Комментарий не найден или недоступен

### Административный API (для модерации)

* `GET /admin/comments` - Получение всех комментариев с возможностью поиска по тексту и сортировки по дате.

    **Ответ:** Если комментарии не найдены, возвращает пустой список

* `DELETE /admin/comments/{comId}` - Удаление любого комментария администратором.

**Ошибки:**

* `404` - Комментарий не найден или недоступен

* `PATCH admin/comments/{comId}?approved=true/false` - Модерация комментария: подтверждение или отклонение.

**Ошибки:**

* `404` - Комментарий не найден или недоступен
* `400` - Комментарий не в статусе `WAITING`

## 📊 Модели данных

### DTO для комментариев

* Создание комментария

```json
{
  "eventId": "id события",
  "text": "текст комментария (maxLength: 1000, minLength: 1)"
}
```

* Обновление комментария

```json
{
  "id": "id комментария",
  "text": "текст комментария (maxLength: 1000, minLength: 1)"
}
```

* Вывод комментария (для пользователей)

```json
{
  "id": "id комментария",
  "author": "имя автора",
  "text": "текст комментария"
}
```

* Вывод комментария (для администратора)

```json
{
  "id": "id комментария",
  "author": "имя автора",
  "text": "текст комментария",
  "state": "статус комментария"
}
```

* Состояния комментария

```java
enum CommentState {
    WAITING,    // ожидает модерации
    APPROVED,   // подтвержден
    REJECTED    // отклонен
}
```

### ER-диаграмма (таблица comments)

```sql
CREATE TABLE IF NOT EXISTS comments
(
    id         BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    author_id  BIGINT        NOT NULL REFERENCES users (id) ON DELETE CASCADE,
    event_id   BIGINT        NOT NULL REFERENCES events (id) ON DELETE CASCADE,
    text       VARCHAR(1000) NOT NULL,
    state      VARCHAR(15)   NOT NULL,
    created_on TIMESTAMP WITHOUT TIME ZONE NOT NULL
);
```

## 🚀 Запуск проекта

### Требования

* Java 21
* Maven 3.8+
* Docker & Docker Compose

### Инструкция по запуску

* Клонируйте репозиторий:

```bash
git clone https://github.com/VitalyVL1/java-explore-with-me-plus.git
cd java-explore-with-me-plus
```

* Соберите проект с помощью Maven:

```bash
mvn clean package
```

* Запустите все сервисы с помощью Docker Compose:

```bash
docker-compose up -d
```

* Остановка проекта:

```bash
docker-compose down
```

## 🏗️ Архитектура проекта

Проект состоит из следующих модулей:

```text
java-explore-with-me-plus/
├── ewm/                   # Основной сервис (EWM)
├── stats-server/          # Сервис статистики
│   ├── stats-client/      # HTTP-клиент для статистики
│   ├── stats-dto/         # Общие DTO для статистики
│   └── stats-server/      # Бизнес-логика сервиса статистики
├── docker-compose.yml     # Оркестрация контейнеров
└── pom.xml                # Родительский POM-файл
```

## 🛠️ Технологический стек

* **Язык:** Java 21
* **Фреймворк:** Spring Boot 3.5.5, Spring Data JPA, Spring Web MVC
* **Сборка:** Maven
* **База данных:** PostgreSQL 42.7.7, H2 2.3.232 (для тестирования)
* **Контейнеризация:** Docker, Docker Compose
* **Документация:** OpenAPI (Swagger)
* **Тестирование:** Postman (коллекции в папке /postman), JaCoCo 0.8.12, SpotBugs 4.8.5.0
* **Утилиты:** Lombok 1.18.30, MapStruct 1.6.3, QueryDSL 5.1.0
* **Статический анализ:** Checkstyle 10.3 (с плагином 3.1.2)

## 📂 Файлы спецификаций

В корне проекта находятся полные OpenAPI спецификации:

* [ewm-main-service-spec.json](ewm-main-service-spec.json) — спецификация для основного сервиса
* [ewm-stats-service-spec.json](ewm-stats-service-spec.json) — спецификация для сервиса статистики

## 🧪 Тестирование

В папке [postman](postman) находятся коллекции Postman для тестирования всего функционала проекта,
включая дополнительную функциональность комментариев.