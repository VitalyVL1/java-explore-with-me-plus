package ru.practicum.dto.user;

import java.util.List;

/**
 * DTO (Data Transfer Object) для параметров поиска пользователей.
 * <p>
 * Используется для передачи параметров фильтрации и пагинации в сервисный слой
 * при запросе списка пользователей. Содержит список идентификаторов для фильтрации
 * и параметры пагинации.
 * </p>
 *
 * @param ids список идентификаторов пользователей для фильтрации (опционально)
 * @param from количество элементов для пропуска (пагинация)
 * @param size количество элементов на странице (пагинация)
 */
public record UserParam(List<Long> ids, Integer from, Integer size) {
}